# .env.example - copy to .env and fill secrets before running
# Application base URL used in redirect-uri values
BASE_URL=http://localhost:8080

# Google OAuth2 credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Naver OAuth2 credentials
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# Database (MariaDB) creds used by docker-compose
MARIADB_ROOT_PASSWORD=root
MARIADB_DATABASE=kraft_db
MARIADB_USER=kraft_user
MARIADB_PASSWORD=Gkstmvns1!

# Spring datasource override for local IDE runs (optional)
SPRING_DATASOURCE_URL=jdbc:mariadb://localhost:3306/kraft_db
SPRING_DATASOURCE_USERNAME=kraft_user
SPRING_DATASOURCE_PASSWORD=Gkstmvns1!

# Flyway baseline
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true
